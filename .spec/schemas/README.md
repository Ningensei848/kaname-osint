# Executable schemas placeholder

Markdown examples in `data-model.md` and `contracts/` are not sufficient as long-term SDD artifacts. This directory is the canonical location for executable JSON Schema files and fixtures used by CI.

Planned schemas:

- `ssot.schema.json`
- `crawler-state.schema.json`
- `topic-frontmatter.schema.json`
- `github-mcp-tool-call.schema.json`
- `cloudflare-pages-deployment.schema.json`
- `discord-embed.schema.json`
